function Card(props) {
    return (
        <div className="Card">
            {/* props

                   |   pin icon | location | map link
            image  |   dates (start - end)
                   |   story/description

            */}
        </div>
    )
}

export default Card;